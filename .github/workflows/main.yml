name: FunLearn AI CI/CD Pipeline

# Yeh pipeline har push aur pull request par chalegi
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  # ── STAGE 1: Django Backend Check ──
  test-backend:
    name: Test Django Backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd backend
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create test environment
        run: |
          cd backend
          echo "DJANGO_SECRET_KEY=test-ci-secret-key-not-real" > .env
          echo "DJANGO_DEBUG=True" >> .env
          echo "DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1" >> .env
          echo "MONGODB_URI=mongodb://localhost:27017/" >> .env
          echo "MONGODB_DB_NAME=funlearn_test" >> .env
          echo "JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60" >> .env
          echo "JWT_REFRESH_TOKEN_LIFETIME_DAYS=7" >> .env
          echo "CORS_ALLOWED_ORIGINS=http://localhost:3000" >> .env

      - name: Django system check
        run: |
          cd backend
          python manage.py check

      - name: Run migrations
        run: |
          cd backend
          python manage.py migrate

  # ── STAGE 2: React Frontend Check ──
  test-frontend:
    name: Test React Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install npm packages
        run: |
          cd frontend
          npm install --legacy-peer-deps

      - name: Build React app
        run: |
          cd frontend
          npm run build
        env:
          CI: false

  # ── STAGE 3: Build Docker Images (Only if Stage 1 & 2 pass) ──
  build-docker:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker
        uses: docker/setup-buildx-action@v2

      - name: Build backend Docker image
        run: |
          cd backend
          echo "DJANGO_SECRET_KEY=test-key" > .env
          echo "MONGODB_URI=mongodb://localhost:27017/" >> .env
          echo "MONGODB_DB_NAME=funlearn_db" >> .env
          docker build -t funlearn-backend:latest .

      - name: Build frontend Docker image
        run: |
          docker build -t funlearn-frontend:latest ./frontend
